#!/bin/sh
export AppName="SmartFarmingApplication"
export Package="com.sdpm.smart.farming"
export UserName="root"
export JavaOpts="-Xmx800m -Xms512m"
bin=`dirname "$0"`
bin=`cd "$bin"; pwd`
"$bin"/daemon.sh "$0" "$@"